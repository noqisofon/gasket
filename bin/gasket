#!/usr/bin/guile \
-s
!#
;; -*- mode: scheme; -*-
;; (use-modules (gascket application))


(define (main args)
  (let ((argc (length args)))
    (display args)
    (newline)
    (display argc)
    (newline)))

(main (command-line))
