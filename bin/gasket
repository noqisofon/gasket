#!/usr/bin/guile \
-e main -s 
!#
;; -*- mode: scheme; -*-
(use-modules (gasket))


(define (main args)
  (let ((argc (length args)))
    (display args)
    (newline)
    (display argc)
    (newline)))

;; (main (command-line))
